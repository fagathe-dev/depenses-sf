{% extends "base.html.twig" %}

{% block body %}
  <h1>Liste des sheets</h1>
  <div class="list-group my-3">
    {% for sheet in sheets %}
      <a class="list-group-item list-group-item-action" href="#">{{ sheet.name }}</a>
    {% endfor %}
  </div>
{% endblock %}